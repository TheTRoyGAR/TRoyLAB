# Copy this file to .env and fill in real values (never commit .env itself).
ANTHROPIC_API_KEY=sk-ant-api03-...
SERPER_API_KEY=...

# TRoyAI backend — used by run_cruises.py (and future run_*.py) to have
# Finance's PRICE_REVIEW skill set the markup/campaign flag per real item,
# instead of a static local table. Same value as TRoyAI's own .env.
BACKEND_API_KEY=...
BACKEND_URL=https://backend.troyaiagent.com
